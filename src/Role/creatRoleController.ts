export const creatRoleController = function (content: any) {
  // 从 context 中获取需要的依赖功能
  const { lendSpawn, remandSpawn, getRoomManager } = content;
  /**
   * 开始采集任务
   */
  const startHaverst = function () {};

  
  /**
   * 执行 tick 逻辑
   */
  const run = function () {};

  return { startHaverst, run };
};
